name: Build and Run

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-18.04

    steps:
    - uses: actions/checkout@v2
    - name: build
      run: g++ -Wall -Wextra -o testchecker *.cpp
      
    - name: run
      run: ./testchecker
